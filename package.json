{
  "name": "coderatingengine",
  "version": "1.0.0",
  "engines": {
    "node": "14.x",
    "yarn": "1.x"
  },
  "description": "Engine to handle the rating mechanisms for the clean code",
  "main": "bin/www",
  "type": "module",
  "scripts": {
    "start": "node --es-module-specifier-resolution=node ./bin/www",
    "test": "mocha --es-module-specifier-resolution=node -r dotenv/config --exit --timeout 15000",
    "dev": "nodemon --es-module-specifier-resolution=node -r dotenv/config ./bin/www"
  },
  "author": "Gaurav Walia",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-rate-limit": "^5.3.0",
    "mongoose": "^5.13.3",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "mocha": "^9.0.2",
    "nodemon": "^2.0.12",
    "supertest": "^6.1.3"
  }
}
